#! minio-minio_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: minio-minio-data
    namespace: vortecomtr
    labels:
        com.docker.compose.service: minio
        com.docker.compose.volume: minio_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
