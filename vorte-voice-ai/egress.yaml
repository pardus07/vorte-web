# LiveKit Egress — Ses kaydı servisi yapılandırması
# API key/secret livekit.yaml ile aynı olmalı (sunucuda güncelleyin)
api_key: devkey
api_secret: secret
ws_url: ws://localhost:7880

redis:
  address: localhost:6379

log_level: info
